{
  "name": "pascal-travels",
  "version": "1.0.0",
  "description": "Pascal Travels & Tours - Premium Travel, Jobs & Visa Consultancy",
  "main": "api/server.js",
  "scripts": {
    "start": "node api/server.js",
    "dev": "nodemon api/server.js",
    "build": "npm install",
    "test": "node --test tests/*.test.js",
    "postinstall": "npm run setup-dirs || true",
    "setup-dirs": "mkdir -p public/assets/images && mkdir -p uploads"
  },
  "dependencies": {
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.7",
    "uuid": "^9.0.1",
    "compression": "^1.7.4",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": "18"
  }
}
